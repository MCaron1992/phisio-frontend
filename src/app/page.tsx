import { Loader } from '@/components/custom /Loader';

const Dashboard = () => {
  return (
    <div>
      <Loader />
    </div>
  );
};

export default Dashboard;

const style = {
  marginLeft: '270px',
  width: '50rem',
  height: '40rem',

  borderRadius: '10px',
};
